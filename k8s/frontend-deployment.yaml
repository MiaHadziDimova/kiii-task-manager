apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend-deployment
  namespace: kiii-task-manager
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
        - name: frontend
          image: miahadjidimova/kiii-task-frontend:latest
          ports:
            - containerPort: 80